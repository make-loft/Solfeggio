<ContentControl
	x:Class="Solfeggio.Views.SolfeggioView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:vm="clr-namespace:Solfeggio.ViewModels"
	xmlns:v="clr-namespace:Solfeggio.Views"
	xmlns:xamarin="http://xamarin.com/schemas/2014/forms"
	DataContext="{Store Key=vm:ProcessingManager}"
	HorizontalContentAlignment="Stretch"
	VerticalContentAlignment="Stretch"
	HorizontalAlignment="Stretch"
	VerticalAlignment="Stretch"
	Background="DimGray">

	<Grid
		Rack.Columns="* 3*"
		Rack.Rows="2* ^ *">

		<Canvas
			Rack.Cell="R0 C0 CS2"
			Name="SpectrumCanvas"
			Background="{DynamicResource SpectrumBackgroundBrush}">

			<Polyline
				Name="MagnitudePolyline"
				Fill="{DynamicResource MagnitudeSpectrumFillBrush.FFT}"
				Stroke="{DynamicResource MagnitudeSpectrumStrokeBrush.FFT}"
				StrokeThickness="{DynamicResource MagnitideSpectrumStrokeThickness}"
				Visibility="{DynamicResource MagnitudeSpectrumVisibility.FFT}"
				/>
			<Polyline
				Name="InterpolatedMagnitudePolyline"
				Fill="{DynamicResource MagnitudeSpectrumFillBrush.PMI}"
				Stroke="{DynamicResource MagnitudeSpectrumStrokeBrush.PMI}"
				StrokeThickness="{DynamicResource MagnitideSpectrumStrokeThickness}"
				Visibility="{DynamicResource MagnitudeSpectrumVisibility.PMI}"
				/>
			<Polyline
				Name="PhasePolyline"
				Stroke="{DynamicResource WaveOutStrokeBrush}"
				StrokeThickness="{DynamicResource WaveInStrokeThickness}"
				Visibility="{DynamicResource PhaseSpectrumVisibility.FFT}"
				/>
			<Polyline
				Name="InterpolatedPhasePolyline"
				Fill="{DynamicResource MagnitudeSpectrumFillBrush.PMI}"
				Stroke="{DynamicResource MagnitudeSpectrumStrokeBrush.PMI}"
				StrokeThickness="{DynamicResource WaveInStrokeThickness}"
				Visibility="{DynamicResource PhaseSpectrumVisibility.PMI}"
				/>
			<Polyline
				Name="WaveInPolyline"
				Fill="{DynamicResource FrameFillBrush.Rectangle}"
				Stroke="{DynamicResource FrameStrokeBrush.Rectangle}"
				StrokeThickness="{DynamicResource WaveInStrokeThickness}"
				Visibility="{DynamicResource FrameVisibility.Rectangle}"
				/>
			<Polyline
				Name="WaveOutPolyline"
				Fill="{DynamicResource FrameFillBrush.Window}"
				Stroke="{DynamicResource FrameStrokeBrush.Window}"
				StrokeThickness="{DynamicResource WaveOutStrokeThickness}"
				Visibility="{DynamicResource FrameVisibility.Window}"
				/>
		</Canvas>

		<GridSplitter
			Rack.Cell="R1 C0 CS2"
			VerticalAlignment="Center"
			HorizontalAlignment="Stretch"
			Background="{DynamicResource SplitterBrush}"
			Height="8" />

		<Canvas
			Rack.Cell="R2 C0 CS2"
			Name="PianoCanvas"
			HorizontalAlignment="Stretch"
			Background="{DynamicResource PianoCanvasBackgroundBrush}" />

		<Grid
			Rack.Cell="R2 C0"
			Rack.Rows="* ^ ^"
			Rack.Columns="^ ^ *">
			<xamarin:Label
				Rack.Cell="R1 C0"
				FontSize="18"
				Opacity="0.7"
				Foreground="IndianRed"
				Text="🎼" />
			<xamarin:Switch
				Rack.Cell="R1 C1"
				Name="OptionsToggleButton"
				IsChecked="False" />
			<xamarin:Label
				Rack.Cell="R2 C0"
				FontSize="18"
				Opacity="0.7"
				Foreground="IndianRed"
				Text="📷" />
			<xamarin:Switch
				Rack.Cell="R2 C1"
				IsChecked="{Binding IsPaused, Mode=TwoWay}" />
		</Grid>

		<Grid
			Rack.Cell="R0 C0 CS2"
			Visibility="{Binding ElementName=OptionsToggleButton, Path=IsChecked, Converter={StaticResource TrueToVisibleConverter}}">
			<Grid>
				<Border
					Background="DimGray"
					Opacity=".5" />
				<v:OptionsView />
			</Grid>
		</Grid>

		<!--<ItemsControl
			Rack.Cell="R0 C0"
			Margin="0 32 0 0"
			Foreground="Violet"
			Name="HarmoncsItemsControl"
			Visibility="{StoreBinding StoreKey=p:MusicalPresenter, Path=VisualProfile.Harmonics, Converter={StaticResource TrueToVisibleConverter}}"
			ItemsSource="{StoreBinding Path=Harmonics, StoreKey=vm:AppViewModel}"
			ItemTemplate="{StaticResource HarmonicOutTemplate}" />-->

	</Grid>

</ContentControl>