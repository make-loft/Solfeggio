<ContentView
	x:Class="Solfeggio.Views.OptionsView"
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:m="clr-namespace:Ace.Markup;assembly=Ace"
	xmlns:presenters="clr-namespace:Solfeggio.Presenters"
	xmlns:viewModels="clr-namespace:Solfeggio.ViewModels"
	xmlns:controls="clr-namespace:Solfeggio.Controls">

	<controls:Pivot>

		<controls:PivotItem
			Header="{m:Localizing Key=Telemetry}">
			<ScrollViewer>
				<StackLayout>
					<Expander
						Header="{m:Localizing Key=Dominants}">
						<ItemsView
							Name="DominatsItemsControl"
							ItemsSource="{m:StoreBinding Path=Dominants, StoreKey=viewModels:AppViewModel}"
							ItemTemplate="{StaticResource DominantTemplate}" />
					</Expander>
					<ItemsView>
						<Entry
							Text="{m:StoreBinding Path=ActiveDevice.Frequency, Mode=TwoWay, StoreKey=viewModels:SpectralViewModel}" />
						<Slider
							Minimum="{m:StoreBinding Path=Frequency.Limit.Lower, StoreKey=presenters:MusicalPresenter}"
							Maximum="{m:StoreBinding Path=Frequency.Limit.Upper, StoreKey=presenters:MusicalPresenter}"
							Value="{m:StoreBinding Path=ActiveDevice.Frequency, Mode=TwoWay, StoreKey=viewModels:SpectralViewModel}" />
					</ItemsView>
				</StackLayout>
			</ScrollViewer>
		</controls:PivotItem>

		<controls:PivotItem
			Header="{m:Localizing Key=Сalibration}">
            <ScrollViewer>
                <ItemsView
                    ItemsSource="{StaticResource CalibrationOptionsSet}" />
            </ScrollViewer>
		</controls:PivotItem>

		<controls:PivotItem
			Header="{m:Localizing Key=Visualization}">
			<ScrollViewer>
				<ItemsView
					ItemsSource="{StaticResource VisualizationOptionsSet}"
					BindingContext="{m:Store Key=presenters:MusicalPresenter}" />
			</ScrollViewer>
		</controls:PivotItem>

		<controls:PivotItem
			Header="{m:Localizing Key=About}">
			<ScrollViewer>
				<Label Text="Makeloft Studio" />
			</ScrollViewer>
		</controls:PivotItem>

	</controls:Pivot>

</ContentView>